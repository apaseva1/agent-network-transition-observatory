# Security

The current R1 experiment is synthetic and requires no external API keys or production data.

Future higher-fidelity experiments should use synthetic credentials/data, isolated execution environments, allowlisted tools, controlled network access, and no uncontrolled external side effects.
